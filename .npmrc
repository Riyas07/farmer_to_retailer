engine-strict=true
# Simplifies dev tooling resolution (eslint/prettier/typescript configs shared across workspace packages)
# for this early-stage monorepo — acceptable per ADR-0002's "lean over complete" principle. Revisit if
# it ever masks a real phantom-dependency bug pnpm's strict default would have caught.
shamefully-hoist=true
