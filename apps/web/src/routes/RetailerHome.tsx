import type { ReactElement } from "react";
export function RetailerHome(): ReactElement {
  return (
    <main>
      <h1>Retailer home (placeholder)</h1>
      <p>Browse/search / orders to receive — see docs/architecture.md §6.</p>
    </main>
  );
}
