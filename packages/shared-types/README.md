# packages/shared-types

Not yet implemented. Will hold DTOs and enums (order status, roles, negotiation status, etc.) shared between
`apps/api`, `apps/web` and `apps/admin` so the state machine and API contracts in `docs/` can't silently drift
between backend and frontend once implementation starts.
